{
  "name": "node-starter",
  "version": "0.0.0",
  "scripts": {
    "start": "serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "serve": "^13.0.2",
    "svg2ttf": "^6.0.3",
    "svgicons2svgfont": "^12.0.0"
  }
}
